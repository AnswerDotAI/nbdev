# config


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Configuring nbdev

nbdev is heavily customizeable, thanks to the configuration system
defined in this module. There are 2 ways to interact with nbdev’s
config:

- **In the terminal:** `nbdev-create-config` creates a `pyproject.toml`
  (if you’re starting a new project use `nbdev-new` instead)
- **In your library:**
  [`get_config`](https://nbdev.fast.ai/api/config.html#get_config)
  returns a
  [`ConfigToml`](https://nbdev.fast.ai/api/config.html#configtoml)
  object.

Read on for more about how these work.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L110"
target="_blank" style="float:right; font-size:smaller">source</a>

### nbdev_create_config

``` python
def nbdev_create_config(
    repo:str=None, # Repo name
    branch:str='main', # Repo default branch
    user:str=None, # Repo username
    author:str=None, # Package author's name
    author_email:str=None, # Package author's email address
    description:str='', # Short summary of the package
    path:str='.', # Path to create config file
    min_python:str='3.10', # Minimum Python version
    license:str='Apache-2.0', # License (SPDX identifier)
):
```

*Create a pyproject.toml config file.*

You can create a `pyproject.toml` by passing settings via the command
line:

``` sh
nbdev-create-config --repo nbdev --user fastai --author fastai \
                    --author-email info@fast.ai --description 'A test project'
```

If you don’t provide settings, we’ll try to infer them from git and
GitHub.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L179"
target="_blank" style="float:right; font-size:smaller">source</a>

### ConfigToml

``` python
def ConfigToml(
    d, proj, cfg_file
):
```

*`dict` subclass that also provides access to keys as attrs, and has a
pretty markdown repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L232"
target="_blank" style="float:right; font-size:smaller">source</a>

### get_config

``` python
def get_config(
    path:NoneType=None, also_settings:bool=False
):
```

*Return nbdev config.*

Searches up from `path` until a `pyproject.toml` with `[tool.nbdev]` is
found. Unspecified optional settings return defaults.

See `nbdev-create-config` for creating a new config.

``` python
cfg = get_config()
```

`cfg` is a
[`ConfigToml`](https://nbdev.fast.ai/api/config.html#configtoml) object
(inherits from `AttrDict`), so you can access keys as attributes:

``` python
p = Path.cwd().parent.parent
test_eq(cfg.lib_name, 'nbdev')
test_eq(cfg.git_url, 'https://github.com/AnswerDotAI/nbdev')
```

Its own path and parent are attributes too:

``` python
test_eq(cfg.config_path, p)
test_eq(cfg.config_file, p/'pyproject.toml')
```

Paths are relative to the project:

``` python
test_eq(cfg.doc_path, p/'_docs')
test_eq(cfg.lib_path, p/'nbdev')
test_eq(cfg.nbs_path, p/'nbs')
```

Note: If no `pyproject.toml` with `[tool.nbdev]` is found,
[`get_config()`](https://nbdev.fast.ai/api/config.html#get_config)
returns a minimal config with defaults based on the current directory.
Use [`is_nbdev()`](https://nbdev.fast.ai/api/config.html#is_nbdev) to
check if you’re in an nbdev project.

You can customize nbdev for all your projects by creating a
`~/.config/nbdev/config.toml` file (or following the [XDG
specification](https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html)).
Any `[tool.nbdev]` key can go there, with the project’s own setting
winning: for example, `jupyter_hooks = false` globally disables nbdev’s
Jupyter hooks, and `cell_timeout = 60` gives `nbdev-test` a default cell
timeout. `nbdev-create-config` also reads `user`, `author`, and
`author_email` from it when neither the arguments nor the git remote
supply them.

## Helpers

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L248"
target="_blank" style="float:right; font-size:smaller">source</a>

### is_nbdev

``` python
def is_nbdev(
    path:NoneType=None
):
```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L251"
target="_blank" style="float:right; font-size:smaller">source</a>

### create_output

``` python
def create_output(
    txt, mime
):
```

*Add a cell output containing `txt` of the `mime` text MIME sub-type*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L257"
target="_blank" style="float:right; font-size:smaller">source</a>

### show_src

``` python
def show_src(
    src, lang:str='python'
):
```

*Call self as a function.*

``` python
show_src("print(create_output('text', 'text/plain'))")
```

<div class="prose" data-markdown="1">

``` python
print(create_output('text', 'text/plain'))
```

</div>

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L260"
target="_blank" style="float:right; font-size:smaller">source</a>

### nbpath2html

``` python
def nbpath2html(
    p
):
```

*Convert notebook path `p` to its html doc path (strips numeric prefix)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L266"
target="_blank" style="float:right; font-size:smaller">source</a>

### nbpath2docurl

``` python
def nbpath2docurl(
    nb_path
):
```

*Hosted docs URL (with `.md` suffix) for notebook `nb_path`, or ’’ if no
`doc_host`*

## Exporting a basic module

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L297"
target="_blank" style="float:right; font-size:smaller">source</a>

### read_version

``` python
def read_version(
    path
):
```

*Read **version** from `path/__init__.py`, or None if not found*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L305"
target="_blank" style="float:right; font-size:smaller">source</a>

### set_version

``` python
def set_version(
    path, version
):
```

*Set **version** in `path/__init__.py`*

``` python
with tempfile.TemporaryDirectory() as d:
    test_is(read_version(d), None)  # No file yet
    set_version(d, '1.0.0')
    test_eq(read_version(d), '1.0.0')
    set_version(d, '1.0.1')
    test_eq(read_version(d), '1.0.1')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L318"
target="_blank" style="float:right; font-size:smaller">source</a>

### bump_version

``` python
def bump_version(
    v, part:NoneType=None, unbump:bool=False
):
```

*Bump `.postN` by default when present, otherwise a semver part counted
from the right*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L334"
target="_blank" style="float:right; font-size:smaller">source</a>

### update_version

``` python
def update_version(
    path:NoneType=None
):
```

*Add **version** to `path/__init__.py` if it doesn’t exist*

``` python
test_eq(bump_version('1.2.3'), '1.2.4')
test_eq(bump_version('1.2.3', part=1), '1.3.0')
test_eq(bump_version('1.2.3', part=0), '2.0.0')
test_eq(bump_version('1.2.3', part=2, unbump=True), '1.2.2')
test_eq(bump_version('2026.05.27.2'), '2026.05.27.3')
test_eq(bump_version('2026.05.27.2', part=1), '2026.05.28.0')
test_eq(bump_version('2026.05.27.2', part=0), '2026.6.0.0')
test_eq(bump_version('0.0.2026082005.post1'), '0.0.2026082005.post2')
test_eq(bump_version('0.0.2026082005.post2', unbump=True), '0.0.2026082005.post1')
test_eq(bump_version('0.0.2026082005.post1', part=2), '0.0.2026082006')
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L342"
target="_blank" style="float:right; font-size:smaller">source</a>

### update_proj

``` python
def update_proj(
    path
):
```

*Create or update `pyproject.toml` in the project root.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L352"
target="_blank" style="float:right; font-size:smaller">source</a>

### add_init

``` python
def add_init(
    path:NoneType=None
):
```

*Add `__init__.py` in all subdirs of `path` containing python files if
it’s not there already.*

An LLM (or person) asking `doc(pkg)` should learn which modules to read
next, like a good `llms.txt`. The curation signal is the docstring
itself: a module saying more than the default summary plus `Docs:` link
has had documentation deliberately written for it, so it’s meant to be
independently useful.
[`pkg_mdoc`](https://nbdev.fast.ai/api/config.html#pkg_mdoc) lists those
modules, each with the first paragraph of its docstring, and
[`update_init_mdoc`](https://nbdev.fast.ai/api/config.html#update_init_mdoc)
writes the result as the package docstring in `__init__.py` (surgically:
any code there is preserved). It runs as part of
[`add_init`](https://nbdev.fast.ai/api/config.html#add_init), so plain
`nbdev-export` keeps it current.

Tools understand the same signal: pyskills’
[`doc()`](https://nbdev.fast.ai/api/showdoc.html#doc) treats a package
with a substantive docstring as curated and elides its mechanical
submodule listing, so the generated `Modules:` list is the whole answer
rather than being repeated (e.g. `doc('pyskills')` itself).

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L414"
target="_blank" style="float:right; font-size:smaller">source</a>

### pkg_mdoc

``` python
def pkg_mdoc(
    path:NoneType=None, desc:NoneType=None, index_nb:NoneType=None
):
```

*Package docstring for `path`: `desc`, intro from `index_nb`, and a line
per module with a substantive docstring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L422"
target="_blank" style="float:right; font-size:smaller">source</a>

### update_init_mdoc

``` python
def update_init_mdoc(
    path:NoneType=None, desc:NoneType=None
):
```

*Write [`pkg_mdoc`](https://nbdev.fast.ai/api/config.html#pkg_mdoc) as
the docstring of `path/__init__.py`, replacing any existing docstring*

The same parts also make a spec-compliant
[llms.txt](https://llmstxt.org): title, `>` summary, the index-notebook
intro, and a `## Modules` section linking each substantive module to its
`.html.md` page (taken from the `Docs:` line of its docstring).
[`update_llms_txt`](https://nbdev.fast.ai/api/config.html#update_llms_txt)
writes it to the notebooks folder, where Quarto copies it to the site
root. A trailing HTML-comment marker identifies the file as generated: a
hand-written `llms.txt` (no marker) is never overwritten.

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L458"
target="_blank" style="float:right; font-size:smaller">source</a>

### update_llms_txt

``` python
def update_llms_txt(
    path:NoneType=None, fn:NoneType=None, desc:NoneType=None, index_nb:NoneType=None
):
```

*Write [`pkg_llms`](https://nbdev.fast.ai/api/config.html#pkg_llms) to
`fn` when it’s absent or generated (has the nbdev marker)*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L448"
target="_blank" style="float:right; font-size:smaller">source</a>

### pkg_llms

``` python
def pkg_llms(
    path:NoneType=None, desc:NoneType=None, index_nb:NoneType=None
):
```

*llms.txt for the package at `path`, from the same sources as
[`pkg_mdoc`](https://nbdev.fast.ai/api/config.html#pkg_mdoc) (’’ if no
intro or modules)*

``` python
with tempfile.TemporaryDirectory() as d:
    d = Path(d)
    cfg_text = (pyproj_tmpl.replace('name = "FILL_IN"', 'name = "testpkg"').replace('requires-python="FILL_IN"', 'requires-python=">=3.10"'))
    cfg_text += '\n[tool.nbdev]\n'
    (d/_pyproj).write_text(cfg_text)
    (d/'testpkg').mkdir()

    with working_directory(d): update_proj(d)

    result = (d/_pyproj).read_text()
    assert 'name = "testpkg"' in result
```

Python modules require a `__init.py__` file in all directories that are
modules. We assume that all directories containing a python file
(including in subdirectories of any depth) is a module, and therefore
add a `__init__.py` to each.

``` python
with tempfile.TemporaryDirectory() as d:
    d = Path(d)
    (d/'a/b').mkdir(parents=True)
    (d/'a/b/f.py').touch()
    (d/'a/c').mkdir()
    add_init(d)
    assert not (d/'a/c'/_init).exists(), "Should not add init to dir without py file"
    for e in [d, d/'a', d/'a/b']: assert (e/_init).exists(),f"Missing init in {e}"
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L469"
target="_blank" style="float:right; font-size:smaller">source</a>

### import_obj

``` python
def import_obj(
    s
):
```

*Import and return `module:obj` string*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/nbdev/blob/main/nbdev/config.py#L476"
target="_blank" style="float:right; font-size:smaller">source</a>

### write_cells

``` python
def write_cells(
    cells, hdr, file, solo_nb:bool=False
):
```

*Write `cells` to `file` along with header `hdr` (mainly for nbdev
internal use).*

This is a simple exporter with just enough functionality to correctly
export this notebook, in order to bootstrap the creation of nbdev
itself.

``` python
# #| hide
# #| eval: false
# path = Path('../nbdev')
# (path/'config.py').unlink(missing_ok=True)
# 
# _basic_export_nb("01_config.ipynb", 'config.py')
# 
# g = exec_new('from nbdev import config')
# assert g['config'].add_init
# assert 'add_init' in g['config'].__all__
```
